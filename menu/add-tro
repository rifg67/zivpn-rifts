#!/bin/bash

# Color Definitions
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
PURPLE='\033[0;35m'
CYAN='\033[0;36m'
WHITE='\033[1;37m'
BG_BLUE='\033[44;1m'
BG_GREEN='\033[42;1m'
BG_RED='\033[41;1m'
NC='\033[0m'
BOLD='\033[1m'

# Function to display header
display_header() {
    clear
    echo -e "${BLUE}┌─────────────────────────────────┐${NC}"
    echo -e "${BLUE}│${YELLOW}    ╔═══════════════════════╗${BLUE}    │${NC}"
    echo -e "${BLUE}│${YELLOW}    ║   🚀 CREATE TROJAN    ║${BLUE}    │${NC}"
    echo -e "${BLUE}│${YELLOW}    ║   Premium Edition     ║${BLUE}    │${NC}"
    echo -e "${BLUE}│${YELLOW}    ╚═══════════════════════╝${BLUE}    │${NC}"
    echo -e "${BLUE}└─────────────────────────────────┘${NC}"
    echo
}

# Get system information
ISP=$(curl -s ipinfo.io/org | cut -d " " -f 2-10 2>/dev/null || echo "Unknown ISP")
CITY=$(curl -s ipinfo.io/city 2>/dev/null || echo "Unknown City")
domain=$(cat /etc/zivpn/domain 2>/dev/null || echo "example.com")
nama=$(cat /etc/zivpn/username 2>/dev/null || echo "PEYX TUNNEL")
IP=$(curl -sS ipv4.icanhazip.com 2>/dev/null || echo "Unknown IP")

# ==================== BOT CONFIGURATION (DARI /etc/zivpn/bot-config.json) ====================
BOT_CONFIG="/etc/zivpn/bot-config.json"

get_bot_config() {
    if [ -f "$BOT_CONFIG" ]; then
        KEY=$(grep -o '"bot_token": "[^"]*"' "$BOT_CONFIG" | cut -d'"' -f4)
        CHATIDS=$(grep -o '"admin_id": [0-9]*' "$BOT_CONFIG" | cut -d' ' -f2)
        echo "$KEY|$CHATIDS"
    else
        echo "|"
    fi
}

BOT_INFO=$(get_bot_config)
KEY=$(echo "$BOT_INFO" | cut -d'|' -f1)
CHATIDS=$(echo "$BOT_INFO" | cut -d'|' -f2)
TIME="10"
URL="https://api.telegram.org/bot$KEY/sendMessage"

# Transaction bot (gunakan bot yang sama)
KEY2="$KEY"
CHATID2="$CHATIDS"
URL2="https://api.telegram.org/bot$KEY2/sendMessage"

# Function to install Trojan monitoring cron job
install_trojan_monitoring_cron() {
    cat > /usr/local/bin/trojan_monitor.sh << 'EOF'
#!/bin/bash

# Configuration - ambil dari /etc/zivpn/bot-config.json
BOT_CONFIG="/etc/zivpn/bot-config.json"
if [ -f "$BOT_CONFIG" ]; then
    KEY=$(grep -o '"bot_token": "[^"]*"' "$BOT_CONFIG" | cut -d'"' -f4)
    CHATIDS=$(grep -o '"admin_id": [0-9]*' "$BOT_CONFIG" | cut -d' ' -f2)
else
    KEY=""
    CHATIDS=""
fi
TIME="10"
URL="https://api.telegram.org/bot$KEY/sendMessage"

get_trojan_connections() {
    local username=$1
    local connections=$(tail -n 100 /var/log/xray/access.log 2>/dev/null | grep "accepted" | grep "trojan" | grep "$username" | awk '{print $3}' | sort | uniq | wc -l)
    echo $connections
}

get_trojan_connected_ips() {
    local username=$1
    tail -n 100 /var/log/xray/access.log 2>/dev/null | grep "accepted" | grep "trojan" | grep "$username" | awk '{print $3}' | sort | uniq | tr '\n' ', ' | sed 's/, $//'
}

tail -n 50 /var/log/xray/access.log 2>/dev/null | grep "accepted" | grep "trojan" | while read line; do
    if [[ $line =~ accepted.*trojan.*email:\ ([^ ]+) ]]; then
        username="${BASH_REMATCH[1]}"
        client_ip=$(echo "$line" | awk '{print $3}')
        
        if [ -f "/etc/peyx/limit/trojan/ip/$username" ]; then
            ip_limit=$(cat "/etc/peyx/limit/trojan/ip/$username" 2>/dev/null)
            
            if [ "$ip_limit" -gt 0 ]; then
                sleep 2
                current_connections=$(get_trojan_connections "$username")
                connected_ips=$(get_trojan_connected_ips "$username")
                
                if [ "$current_connections" -gt "$ip_limit" ]; then
                    TEXT="
<code>------------------------------</code>
<b>⚠️ TROJAN IP LIMIT EXCEEDED</b>
<code>------------------------------</code>
<code>Username      : $username</code>
<code>Current IPs   : $current_connections</code>
<code>Allowed IPs   : $ip_limit</code>
<code>Client IP     : $client_ip</code>
<code>All IPs       : $connected_ips</code>
<code>Time          : $(date)</code>
<code>------------------------------</code>"
                    
                    for CHATID in $CHATIDS; do
                        curl -s --max-time "$TIME" -d "chat_id=$CHATID&disable_web_page_preview=1&text=$TEXT&parse_mode=html" "$URL" >/dev/null
                    done
                    echo "$(date): Trojan IP Limit Exceeded - User: $username" >> /var/log/trojan_ip_limit.log
                fi
            fi
        fi
    fi
done

if [ "$(date +%M)" = "00" ]; then
    today=$(date +"%Y-%m-%d")
    if [ -f "/etc/peyx/trojan.db" ]; then
        while IFS= read -r line; do
            if [[ "$line" =~ ^###[[:space:]]+([^[:space:]]+)[[:space:]]+([^[:space:]]+)[[:space:]]+([^[:space:]]+)[[:space:]]+([^[:space:]]+)[[:space:]]+([^[:space:]]+) ]]; then
                username="${BASH_REMATCH[1]}"
                expire_date="${BASH_REMATCH[2]}"
                
                if [ "$expire_date" = "$today" ]; then
                    TEXT="
<code>------------------------------</code>
<b>🔔 TROJAN ACCOUNT EXPIRED</b>
<code>------------------------------</code>
<code>Username      : $username</code>
<code>Status        : EXPIRED TODAY</code>
<code>Expiry Date   : $expire_date</code>
<code>Time          : $(date)</code>
<code>------------------------------</code>"
                    
                    for CHATID in $CHATIDS; do
                        curl -s --max-time "$TIME" -d "chat_id=$CHATID&disable_web_page_preview=1&text=$TEXT&parse_mode=html" "$URL" >/dev/null
                    done
                    echo "$(date): Trojan Account Expired - User: $username" >> /var/log/trojan_expiry.log
                fi
            fi
        done < /etc/peyx/trojan.db
    fi
fi
EOF

    chmod +x /usr/local/bin/trojan_monitor.sh
    (crontab -l 2>/dev/null | grep -v "/usr/local/bin/trojan_monitor.sh"; echo "* * * * * /usr/local/bin/trojan_monitor.sh") | crontab -
    touch /var/log/trojan_ip_limit.log /var/log/trojan_expiry.log
    chmod 644 /var/log/trojan_ip_limit.log /var/log/trojan_expiry.log
}

# Function to send Telegram notification
send_telegram_notification() {
    local message="$1"
    local is_transaction="$2"
    
    local target_chatid="$CHATIDS"
    local target_url="$URL"
    
    if [ "$is_transaction" = "true" ]; then
        target_chatid="$CHATID2"
        target_url="$URL2"
    fi
    
    if [ -n "$target_chatid" ] && [ -n "$KEY" ]; then
        curl -s --max-time "$TIME" -X POST "$target_url" \
            -d "chat_id=${target_chatid}" \
            -d "disable_web_page_preview=1" \
            -d "parse_mode=html" \
            --data-urlencode "text=${message}" >/dev/null
    fi
}

# Function to create Trojan account
Create_Trojan_Account() {
    display_header
    echo -e "\n${BG_GREEN}${WHITE} CREATE TROJAN ACCOUNT ${NC}"
    echo -e "${BLUE}────────────────────────────${NC}"
    echo
    
    echo -e "${CYAN}Installing monitoring service...${NC}"
    install_trojan_monitoring_cron
    echo -e "${GREEN}✓ Monitoring service installed${NC}"
    
    echo -e "${CYAN}╭─ ACCOUNT DETAILS ${NC}"
    echo -e "${CYAN}╰──────────────────${NC}"
    echo -e "${WHITE}Enter account information${NC}"
    
    # Username input
    while true; do
        echo -ne "${PURPLE}➤ Username: ${NC}"
        read user
        
        if [[ -z "$user" ]]; then
            echo -e "${RED}❌ Error: Username cannot be empty!${NC}"
            continue
        fi
        
        if grep -q "### $user" /etc/peyx/trojan.db 2>/dev/null; then
            echo -e "${RED}❌ Error: Username already exists!${NC}"
            continue
        fi
        break
    done
    
    # Password input
    echo -ne "${PURPLE}➤ Custom Password (press enter for random): ${NC}"
    read input_pass
    if [[ -z $input_pass ]]; then
        password=$(cat /proc/sys/kernel/random/uuid)
    else
        password=$input_pass
    fi
    
    echo -e "\n${YELLOW}Note: Enter 0 for unlimited${NC}"
    echo -ne "${PURPLE}➤ Quota (GB): ${NC}"
    read Quota
    [[ -z "$Quota" ]] && Quota=0
    
    echo -ne "${PURPLE}➤ Limit IP: ${NC}"
    read iplimit
    [[ -z "$iplimit" ]] && iplimit=0
    
    echo -ne "${PURPLE}➤ Duration (days): ${NC}"
    read masaaktif
    
    if ! [[ "$masaaktif" =~ ^[0-9]+$ ]] || [ "$masaaktif" -lt 1 ]; then
        echo -e "\n${RED}❌ Error: Please enter valid days!${NC}"
        sleep 2
        return 1
    fi
    
    echo -e "${CYAN}Creating Trojan account...${NC}"
    
    # Set dates
    exp=$(date -d "$masaaktif days" +"%Y-%m-%d")
    expe=$(date -d "$masaaktif days" +"%d %b %Y")
    tnggl=$(date +"%d %b %Y")
    
    # ==================== TAMBAH USER KE XRAY (PAKAI JQ) ====================
    echo -e "${CYAN}Adding user to Xray config...${NC}"
    
    if ! grep -q '"protocol": "trojan"' /usr/local/etc/xray/config.json; then
        echo -e "${RED}❌ Error: Trojan inbound not found in config!${NC}"
        return 1
    fi
    
    jq --arg pass "$password" --arg user "$user" \
       '.inbounds |= map(if .protocol == "trojan" then 
           .settings.clients += [{"password": $pass, "email": $user}] 
       else . end)' /usr/local/etc/xray/config.json > /usr/local/etc/xray/config.json.tmp && \
       mv /usr/local/etc/xray/config.json.tmp /usr/local/etc/xray/config.json
    
    if [ $? -ne 0 ]; then
        echo -e "${RED}❌ Error: Failed to add user to config!${NC}"
        return 1
    fi
    
    systemctl restart xray
    
    # Create Trojan links
    trojan_tls="trojan://${password}@${domain}:443?security=tls&type=ws&path=/trojan&sni=${domain}#${user}"
    trojan_http="trojan://${password}@${domain}:80?security=none&type=ws&path=/trojan#${user}"
    trojan_grpc="trojan://${password}@${domain}:443?security=tls&type=grpc&serviceName=trojan-grpc&sni=${domain}#${user}"
    
    # Create account info file
    mkdir -p /var/www/html
    cat >/var/www/html/trojan-$user.txt <<-END
==============================
     TROJAN ACCOUNT
==============================
Username     : $user
Password     : $password
Quota        : $Quota GB
IP Limit     : $iplimit Devices
Domain       : $domain
ISP          : $ISP
City         : $CITY
==============================
     CONNECTION LINKS
==============================
Link TLS     : ${trojan_tls}
Link HTTP    : ${trojan_http}
Link GRPC    : ${trojan_grpc}
==============================
Created      : $tnggl
Expired      : $expe
Duration     : $masaaktif Days
==============================
END

    # Set IP limit and quota
    if [[ $iplimit -gt 0 ]]; then
        mkdir -p /etc/peyx/limit/trojan/ip
        echo "$iplimit" > /etc/peyx/limit/trojan/ip/$user
        echo -e "${GREEN}✅ IP limit set: $iplimit device(s)${NC}"
    fi

    mkdir -p /etc/peyx/trojan
    if [[ $Quota -gt 0 ]]; then
        echo $(($Quota * 1024 * 1024 * 1024)) > /etc/peyx/trojan/$user
    fi

    # Save to database
    echo "### $user $exp $password $Quota $iplimit" >> /etc/peyx/trojan.db

    echo -e "${GREEN}✓ Trojan account created${NC}"
    echo -e "${CYAN}Sending notifications...${NC}"
    
    # Notifikasi Telegram
    TEXT="
<b>-----------------------------------------</b>    
<b> Peyx Tunneling Script</b>
<b>-----------------------------------------</b>
<b>           XRAY / TROJAN</b>
<b>-----------------------------------------</b>
<code>Username  : ${user}
Domain    : ${domain}
Quota     : ${Quota} GB
Limit IP  : ${iplimit} Devices
Password  : ${password}</code>
<b>-----------------------------------------</b>
<b>🔗 TROJAN WS TLS</b>
<code>${trojan_tls}</code>
<b>-----------------------------------------</b>
<b>🔗 TROJAN WS HTTP</b>
<code>${trojan_http}</code>
<b>-----------------------------------------</b>
<b>📅 Expired : ${expe}</b>
<b>-----------------------------------------</b>"

    send_telegram_notification "$TEXT" "false"

    # Notifikasi transaksi
    TEXT2="
<b>-----------------------------------------</b>
   <b>TRANSAKSI BERHASIL</b>
<b>-----------------------------------------</b>
<b>» Produk : Trojan</b>
<b>» ISP :</b> ${ISP}
<b>» Domain :</b> ${domain}
<b>» Region :</b> ${CITY}
<b>» User :</b> ${user}
<b>» Aktif :</b> ${masaaktif} Hari
<b>» Limit IP :</b> ${iplimit} Devices
<b>» Dibuat   :</b> ${tnggl}
<b>» Berakhir :</b> ${expe}
<b>-----------------------------------------</b>"
    
    send_telegram_notification "$TEXT2" "true"
    
    echo -e "${GREEN}✓ Notifications sent${NC}"
    
    clear
    
    # Display result
    echo -e "\n${GREEN}✅ Trojan Account Created Successfully!${NC}"
    echo -e "${BLUE}------------------------------${NC}"
    echo -e "${WHITE}     ACCOUNT DETAILS${NC}"
    echo -e "${BLUE}------------------------------${NC}"
    echo -e "${CYAN}Username         ${WHITE}: ${GREEN}$user${NC}"
    echo -e "${CYAN}Password         ${WHITE}: ${GREEN}$password${NC}"
    echo -e "${CYAN}Quota            ${WHITE}: ${YELLOW}$Quota GB${NC}"
    echo -e "${CYAN}IP Limit         ${WHITE}: ${YELLOW}$iplimit Device(s)${NC}"
    echo -e "${CYAN}Duration         ${WHITE}: ${YELLOW}$masaaktif Days${NC}"
    echo -e "${CYAN}Domain           ${WHITE}: ${GREEN}$domain${NC}"
    echo -e "${BLUE}------------------------------${NC}"
    
    echo -e "\n${WHITE}     CONFIGURATION INFO${NC}"
    echo -e "${BLUE}------------------------------${NC}"
    echo -e "${CYAN}Link TLS (443)   ${WHITE}: ${GREEN}$trojan_tls${NC}"
    echo ""
    echo -e "${CYAN}Link HTTP (80)   ${WHITE}: ${GREEN}$trojan_http${NC}"
    echo ""
    echo -e "${CYAN}Link GRPC        ${WHITE}: ${GREEN}$trojan_grpc${NC}"
    echo -e "${BLUE}------------------------------${NC}"
    
    echo -e "\n${WHITE}        EXPIRY INFO${NC}"
    echo -e "${BLUE}------------------------------${NC}"
    echo -e "${CYAN}Created          ${WHITE}: ${GREEN}$tnggl${NC}"
    echo -e "${CYAN}Expires          ${WHITE}: ${YELLOW}$expe${NC}"
    echo -e "${BLUE}------------------------------${NC}"
    
    echo -e "\n${GREEN}📢 Notifications sent to Telegram!${NC}"
    echo -e "\n${BLUE}------------------------------${NC}"
    echo -e "${PURPLE}        Script By${NC}"
    echo -e "${YELLOW}         $nama${NC}"
    echo -e "${BLUE}------------------------------${NC}"
    
    echo -e "\n${CYAN}Press Enter to continue...${NC}"
    read -n 1 -s
}

# Main execution
Create_Trojan_Account